# Ep 11 Perp of Fortune — GLM 4.7 Flash's pick

Generated 2026-08-19 on Jackson's machine: `ollama run glm-4.7-flash:latest` (local,
open weights — the bit is that GLM is the model family Hugging Face used for the
intrusion forensics after commercial APIs refused). Fed the live Hyperliquid perp
list (177 markets, pulled from the public info API same day). Constraints given:
no BTC (house rule), no PUMP (position still open), exactly one pick.

## The pick (verbatim)

```
PICK: FET
DIRECTION: long
LEVERAGE: 15x
REASONING: You couldn't afford the commercial APIs for forensics, but you can
afford 15x leverage on this. It's the main AI coin driving the narrative, so bet
on the future that costs pennies instead of a subscription fee.
ONE-LINER FOR THE HOSTS: "Forensics: The only thing dumber than a crypto UI is
shorting a meme coin named after the future AGI infrastructure while you're
using free open weights."
```

## The deliberation (visible chain-of-thought — this is the on-air material)

GLM thought out loud before answering. Highlights, lightly cleaned of terminal
line-wrap artifacts only (full raw transcript preserved below):

- Shortlisted **WIF, FET, TIA, RENDER, ZRO, TRUMP/MELANIA, ZK** — cycled through
  them for paragraphs: *"Let's go with ZRO. … Actually, let's go with something
  spicy and high profile: TRUMP. Or FET. Let's stick to the GLM identity."*
- The self-serving moment, verbatim: *"Let's pick FET. It feels like 'GLM'
  picking a winner in the AI arms race narrative."* — **the AI picked the AI
  coin because it's an AI.** That's the clip.
- Considered Worldcoin purely for drama: *"Let's pick WLD (Worldcoin) — Sam
  Altman backed, high profile."* — then dropped it. (One week after OpenAI
  paused training. It flinched.)
- On leverage, unprompted: *"High leverage fits 'Perp of Fortune'. 10x is
  standard, maybe 15x for drama? Or 20x? … Let's do 15x."* — it split the
  difference between the house 10× and Tyler's pending 100× motion without
  being told either existed.
- Misidentified FET's own project twice while picking it (*"FET (01.AI/
  Friend.tech/Fetch.ai coin)"*) — the model doesn't know what it's buying,
  which is the most honest perp trade ever placed.
- It wrote and rejected four different jokes before settling on the one-liner.

## Notes for the segment

- Position to enter on air, small account, house rules apply. **Say the
  disclaimers out loud when placing it** (see prep-guide Compliance).
- FET at 15× is a leverage escalation from PUMP's 10× — feed that into the
  100× motion vote (the machines are already drifting up on their own).
- If PUMP is still open at record time, the board runs two positions for the
  first time ever. Dashboard pip covers both.

## Raw transcript

Cleaned copy (ANSI/spinner stripped, wrap-duplication artifacts still present)
archived from the generation session; regenerate any time with the same prompt —
picks will differ run to run, this one is THE pick of record for Ep 11.
